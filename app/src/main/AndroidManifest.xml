<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vivek.attendanceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginAdminActivity" />
        <activity android:name=".adminFunction" />
        <activity android:name=".facultyRegister" />
        <activity android:name=".studentRegister" />
        <activity android:name=".uploadNotice" />
        <activity android:name=".studentProfile" />
        <activity android:name=".facultyLoginActivity" />
        <activity android:name=".facultyFunction" />
        <activity android:name=".takeAttendance" />
        <activity android:name=".studentLoginActivity" />
        <activity android:name=".studentFunction"></activity>
    </application>

</manifest>